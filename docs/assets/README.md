# Marketing assets

Drop UI screenshots and GIFs here, then wire them into the root README.

Suggested captures (light theme, both languages):

- `hero.png` — the wizard on Step 1 or 2 (referenced by the root README hero).
- `wizard-he.png` / `wizard-en.png` — Hebrew (RTL) and English (LTR) side by side.
- `run.gif` — a short clip of a dry-run: Source → Options → Run → Done.

Keep images reasonably sized (≤ ~1280px wide) so the README stays light.
